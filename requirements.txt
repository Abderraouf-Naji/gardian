# Core deep-learning stack.
# NOTE: install torch FIRST with the CUDA-12.1 wheel index, otherwise pip will
# fall back to the CPU build. See the env setup commands in the project docs:
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
transformers>=4.41.0
bitsandbytes>=0.43.0; sys_platform != "darwin"
accelerate>=0.30.0
sentence-transformers>=2.7.0
# Required for MonoT5 / T5 cross-encoder tokenizers (AutoTokenizer falls back to tiktoken without it).
sentencepiece>=0.2.0
protobuf>=4.25.0

# Datasets / hub.
datasets>=2.18.0
huggingface_hub>=0.24.0

# Retrieval.
bm25s>=0.2.0
# CPU build is enough: rank-data uses torch CUDA matmul when faiss-gpu is absent.
# Optional native GPU: pip install faiss-gpu-cu12  OR  conda install -c conda-forge faiss-gpu
faiss-cpu>=1.7.4

# Graph & ML utilities.
networkx>=3.2
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0

# Config & I/O.
omegaconf>=2.3.0
loguru>=0.7.0
tqdm>=4.66.0

# NLP utilities.
nltk>=3.8.0

# Plots (scripts/09_plot_training_curves.py).
matplotlib>=3.8.0

# Tests.
pytest>=8.0.0
